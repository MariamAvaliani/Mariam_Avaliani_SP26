# Target used by Tests/test_powerbi_report.py + Pages/capture_report_views_page.py.
#
# NOTE: the DQE_LAB_2024 skeleton's own config pointed at a real Microsoft
# Investor Relations earnings page with an embedded Power BI "tab". We
# checked that page live (2026-09-23) and Microsoft has since removed that
# Power BI tab entirely - it no longer exists on the page. So instead we
# use the exact target the task's own instructions (Data_Quality_task13...
# .docx) point to: the Power BI Playground's "Capture report views"
# showcase, which is a real, currently-live embedded Power BI report.
global:
  report_uri: https://playground.powerbi.com/en-us/showcases-gallery/capture-report-views
  delay: 20
